---
permalink: /news/
---

<!DOCTYPE html>
<html lang="en">

<head>
    <title>NAPSS Unilorin | News</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/favicon-32x32.png" type="image/png">
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700&family=Roboto:wght@400;500;700&display=swap"
        rel="stylesheet">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Styles */
        body {
            font-family: 'Roboto', Arial, Helvetica, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        /* Navbar */
        .navbar {
            background-color: #004D25;
            color: white;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .foo {
            display: flex;
            align-items: center;
        }

        .foo img {
            max-width: 60px;
            max-height: 60px;
            margin-right: 5px;
        }

        .foo h1 {
            margin: 0;
            font-size: 24px;
        }

        .dynamic-content {
            flex: 1;
            text-align: center;
        }

        .dynamic-content h2 {
            font-size: 1.2em;
            margin: 0;
        }

        .bar a {
            border-radius: 5px;
            padding: 10px 20px;
            font-size: 20px;
            cursor: pointer;
            background-color: #FFC107;
            color: #004D25;
            transition: background-color 0.3s ease;
            border: none;
            text-decoration: none;
        }

        .bar a:hover {
            background-color: #E6B800;
        }

        /* News Hero Section */
        .news-hero {
            background-image: url('/img/departmental-building.png');
            background-size: cover;
            background-position: center;
            height: 250px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            position: relative;
        }

        .news-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 77, 37, 0.6);
            z-index: 1;
        }

        .news-hero h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 4em;
            font-weight: 700;
            position: relative;
            z-index: 2;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        /* News Cards */
        .container.news-section {
            padding: 40px 20px;
        }

        .latest-news-heading {
            text-align: left;
            font-weight: bold;
            font-size: 1.25rem;
            margin-bottom: 20px;
            color: #333;
        }

        .news-articles .card {
            margin-bottom: 30px;
            border: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .news-articles .card:hover {
            transform: translateY(-5px);
        }

        .news-articles .card img {
            height: 200px;
            object-fit: cover;
            width: 100%;
        }
        
        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100%;
        }

        .card-title {
            font-weight: bold;
            color: #004D25;
        }

        .card-text {
            color: #666;
        }

        .read-more-link {
            display: block;
            margin-top: auto;
            background-color: #004D25;
            color: white;
            padding: 10px 15px;
            border-radius: 5px;
            text-align: center;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .read-more-link:hover {
            background-color: #E6B800;
            color: #004D25;
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .navbar {
                flex-direction: row;
                justify-content: space-between;
            }

            .foo {
                align-items: center;
            }

            .dynamic-content {
                display: none;
            }
            
            .news-hero {
                height: 180px;
            }

            .news-hero h1 {
                font-size: 3em;
            }
            
            /* Add some padding to news articles on mobile to prevent them from touching the screen edges */
            .news-article {
                padding: 0 10px;
            }
        }
    </style>
</head>

<body>
    <div class="navbar">
        <div class="foo">
            <img src="/img/napss-logo.jpg" alt="NAPSS Logo">
            <h1>NAPSS Unilorin</h1>
        </div>
        <div class="dynamic-content" id="dynamic-content">
            <h2>(Unilorin Chapter)</h2>
        </div>
        <div class="bar">
            <a href="/homepage.html">Home</a>
        </div>
    </div>

    <div class="news-hero">
        <h1>NEWS</h1>
    </div>

    <div class="container news-section">
        <h2 class="latest-news-heading">Latest News</h2>
        <div class="row news-articles">
            {% for article in collections.news %}
            <div class="col-6 col-md-6 col-lg-4 news-article">
                <div class="card">
                    <img src="/{{ article.data.image }}" class="card-img-top" alt="{{ article.data.title }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.data.title }}</h5>
                        <p class="card-text">{{ article.data.excerpt }}</p>
                        <a href="{{ article.url }}" class="read-more-link">Read more</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const messages = [
            "National Association of Political Science Students (NAPSS) Unilorin",
            "...towards National Consciousness & Democratic Advancement.",
            "Welcome to NAPSS Unilorin Official Website!"
        ];
        let currentIndex = 0;

        function changeMessage() {
            const dynamicContent = document.getElementById("dynamic-content");
            dynamicContent.innerHTML = `<h2>${messages[currentIndex]}</h2>`;
            currentIndex = (currentIndex + 1) % messages.length;
        }
        changeMessage();
        setInterval(changeMessage, 3500);
    </script>
</body>

</html>